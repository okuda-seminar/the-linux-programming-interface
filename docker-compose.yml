version: "3"
services:
  the_linux_programming_interface:
    build:
      context: .
    container_name: the_linux_programming_interface
    environmrnt:
      - C_INCLUDE_PATH=/workspace/include
    privileged: true
    tty: true
    volumes:
      - ./:/workspace/
    